# Codex Project Notes

This project intentionally uses repo `AGENTS.md` and repo-scoped skills in
`.agents/skills/` as the active agent surfaces.

`hooks.example.json` is provided as a template only. Rename it to `hooks.json`
after reviewing it if you want Codex lifecycle hooks in this repository.
